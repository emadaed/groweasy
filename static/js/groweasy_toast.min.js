// Enhanced Toast with Fun Messages
function showToast(message, bgColor = '#28a745', duration = 3000) {
    const toast = document.createElement('div');
    toast.style.cssText = `
        position: fixed;
        top: 80px;
        right: 20px;
        background: {{ currency_symbol }}{bgColor};
        color: white;
        padding: 16px 24px;
        border-radius: 12px;
        box-shadow: 0 8px 24px rgba(0,0,0,0.2);
        z-index: 10000;
        font-size: 15px;
        font-weight: 500;
        min-width: 300px;
        animation: slideInRight 0.3s ease-out;
    `;
    toast.innerHTML = message;
    document.body.appendChild(toast);

    setTimeout(() => {
        toast.style.animation = 'slideOutRight 0.3s ease-out';
        setTimeout(() => toast.remove(), 300);
    }, duration);
}

// Add animations
const style = document.createElement('style');
style.textContent = `
    @keyframes slideInRight {
        from { transform: translateX(400px); opacity: 0; }
        to { transform: translateX(0); opacity: 1; }
    }
    @keyframes slideOutRight {
        from { transform: translateX(0); opacity: 1; }
        to { transform: translateX(400px); opacity: 0; }
    }
`;
document.head.appendChild(style);

// Fun success messages
const successMessages = {
    invoice_created: [
        "🎉 Invoice created! You're a billing boss!",
        "💰 Cha-ching! Another invoice done!",
        "✨ Invoice magic complete!",
        "🚀 Invoice sent to the moon!",
        "🎊 You're on fire! Invoice created!"
    ],
    stock_updated: [
        "📦 Stock updated! Inventory ninja at work!",
        "✅ Stock levels looking good!",
        "🎯 Bullseye! Stock updated perfectly!",
        "💪 Stock management on point!"
    ],
    login_success: [
        "🎉 Welcome back, superstar!",
        "👋 Great to see you again!",
        "✨ You're logged in! Let's make money!",
        "🚀 Ready to conquer the day?"
    ],
    settings_saved: [
        "⚙️ Settings saved! You're all set!",
        "✅ Changes locked in!",
        "💾 Saved! Your preferences are safe!",
        "🎯 Configuration complete!"
    ],
    product_added: [
        "📦 Product added! Your inventory grows!",
        "✨ New product in the house!",
        "🎉 Inventory expanded successfully!",
        "💪 Another product conquered!"
    ]
};

function showSuccessToast(type = 'default', customMessage = null) {
    const messages = successMessages[type] || successMessages.invoice_created;
    const message = customMessage || messages[Math.floor(Math.random() * messages.length)];
    showToast(message, '#28a745', 4000);
}

function showErrorToast(message) {
    showToast('❌ ' + message, '#dc3545', 4000);
}

function showWarningToast(message) {
    showToast('⚠️ ' + message, '#ffc107', 4000);
}

function showInfoToast(message) {
    showToast('ℹ️ ' + message, '#17a2b8', 3000);
}

// Make globally available
window.showToast = showToast;
window.showSuccessToast = showSuccessToast;
window.showErrorToast = showErrorToast;
window.showWarningToast = showWarningToast;
window.showInfoToast = showInfoToast;